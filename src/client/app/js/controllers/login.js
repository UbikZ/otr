'use strict';

var toastr = require('toastr');

module.exports = ['$scope', 'authService',
  function ($scope) {
    $scope.login = function () {

    };

    $scope.logout = function() {

    }
  }
];